let arr=[1,2,3,4,5];
arr.unshift(0);   //add element in the beginning
arr.push(6);   //add element in the last
arr.shift();   //remove  the first element
arr.pop();     //remove the last element
const array=arr.has(2);
console.log(array)